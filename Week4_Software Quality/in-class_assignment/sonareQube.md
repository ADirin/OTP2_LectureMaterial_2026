# OTP2 – Week 4: In-Class Assignment

**Spring 2025**  
**Instructor:** Amir Dirin  

---

> ⚠️ **Important Note**  
> These instructions were initially generated by AI and later revised by the teacher.  
> You may need to adjust your system configuration depending on your environment.

> ⚠️ **JDK Requirement**  
> SonarQube works best with **JDK 17**.  
> If the scanner is not working:
> - Ensure `JAVA_HOME` is set to **JDK 17**

---

## 🎯 Objective

Set up:
- SonarQube  
- SonarScanner  
- IntelliJ integration  

You are **NOT required to submit results**, but everything must work on your machine by:

📅 **Thursday 13.11.2025**

---

## ✅ Prerequisites

- Java **JDK 17+**
- SonarQube (Community Edition or higher)
- SonarScanner
- IntelliJ IDEA (Community or Ultimate)
- (Optional) Maven or Gradle project

---

## 🧩 Step 1: Install SonarQube

### 1.1 Download SonarQube

- Go to:  
  https://www.sonarsource.com/products/sonarqube/downloads/
- Download the **latest stable version**
- Choose the ZIP file for your OS

---

### 1.2 Extract and Run (Windows)

1. Extract ZIP to a folder, e.g.:
```
C:\SonarQube
```
2. Open terminal (CMD / PowerShell)
3. Navigate to:
```
C:\sonarqube\bin\windows-x86-xx\
```
4. Start SonarQube:
```
📌 Example:
```
C:\Users...\sonarqube-25.x\bin\windows-x86-64> StartSonar.bat
```

---

### 🌐 Access SonarQube

Open in browser:
```
http://localhost:9000
```

🔐 Default login:
- Username: `admin`
- Password: `admin`

---

## 🔍 Step 2: Install SonarScanner

### 2.1 Download

https://docs.sonarsource.com/sonarqube/latest/analysis/scan/sonarscanner/

---

### 2.2 Setup

1. Extract to:

```
C:\sonarscanner
```

2. Add to **Environment Variables**

#### ➤ PATH
```
C:\sonarscanner\bin
```

#### ➤ New Variable
- Key: `SONAR_SCANNER_HOME`
- Value: `C:\sonarscanner`

---

## 💻 Step 3: Prepare Project in IntelliJ

- Open your project in IntelliJ  
- Use **Maven or Gradle** (recommended)

---

### 📄 Create `sonar-project.properties`

In the root project folder:

```
sonar.projectKey=your_project_key
sonar.projectName=Your Project Name
sonar.projectVersion=1.0
sonar.sources=src
sonar.java.binaries=target/classes
sonar.login=YOUR_TOKEN
```
sonar.projectKey=your_project_key
sonar.projectName=Your Project Name
sonar.projectVersion=1.0
sonar.sources=src
sonar.java.binaries=target/classes
sonar.login=YOUR_TOKEN
```
sonar-scanner
```

---

### 📊 View Results

After analysis:
```
http://localhost:9000/projects
```

---

## 🧪 Example Java Code

```java
package org.example;

import java.util.logging.Level;
import java.util.logging.Logger;

public class Main {

    private static final Logger logger = Logger.getLogger(Main.class.getName());

    public static void main(String[] args) {

        int result = addMe(12, 4);

        if (logger.isLoggable(Level.INFO)) {
            logger.info(String.format("Result of addMe(12, 4): %d", result));
        }
    }

    public static int addMe(int a, int b) {
        return a + b;
    }
}
```
⚠️ Known Issue

- Code may pass in IntelliJ SonarQube plugin
- But fail in localhost dashboard

💡 Possible fix:
- Add test coverage code

🔗 Test Repository

You can test setup using:
https://github.com/ADirin/SonarQube-_SonarScanner.git

🐞 Potential Errors & Fixes
❌ SonarScanner not working

✔ Check Java version:
```
where java
```
✔ Ensure:

- JAVA_HOME = JDK 17
- Correct version is in System PATH

🏁 Summary

You should have:
- SonarQube running at localhost:9000
- SonarScanner configured
- Project analyzed successfully
